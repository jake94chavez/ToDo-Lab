<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <div class='container'>
    	<% db.Todo.forEach((todo) => { %>
    <div class='todo'>
      <h3 class='todo-name'><%= db.Todo.task %></h3>
    </div>
<%  }) %>
  </body>
</html>
